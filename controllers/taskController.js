/* 
Task Management & Time Tracking (/api/tasks)
1) GET /api/tasks (Get all tasks for the user, filtered by project ID)

2) POST /api/tasks (Create a new task)

3) PATCH /api/tasks/:id (Update a task)

4) DELETE /api/tasks/:id (Delete a task)

5) POST /api/tasks/:id/start (Start a timer for a task)

6) POST /api/tasks/:id/stop (Stop the timer and save the time entry)
*/
